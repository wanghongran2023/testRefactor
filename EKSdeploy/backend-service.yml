apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend-service
spec:
  selector:
    app: backend-reverseproxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
